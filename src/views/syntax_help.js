import React from "react";
import ReactDOM from "react-dom";

class SyntaxHelper extends React.Component {
  render() {
    return (
      <div>
        <p>
          This should something that you can "call" and contains the syntax and
          examples, and a search function for the help
        </p>
      </div>
    );
  }
}

export default SyntaxHelper;
